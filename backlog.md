# Backlog

The scheduler picks the next unchecked task each cycle. Add tasks as `- [ ] …`; the loop checks them
off and moves them under `## Done` when their gate passes.

- [ ] Loop smoke test: run `./gate.sh`, confirm `RESULT: PASS`, and append a one-line
      "instance bootstrapped <date>" note to the END of `NOTES.md`. Starter task — it proves the
      work → gate → commit → memory pipeline end to end. Delete it once it has passed.

## Done
