# VISION — your instance's north star

_Director-owned. Replace this with what THIS Apkallu instance is for: the goal it optimizes, the
decision filter it applies, and what it explicitly will not do._

This file is read-only to workers (the operating guide points them here for "why"). Keep it short
and load-bearing — it is the tie-breaker when a decision is ambiguous.
